<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.fragment.TimesheetListFragment"
    android:orientation="vertical"
    tools:ignore="ExtraText">

        <TextView
            android:id="@+id/timesheet_list_header"
            android:layout_width="fill_parent"
            android:layout_height="wrap_content"
            android:background="@color/cardview_dark_background"
            android:gravity="center_vertical"
            android:padding="5dp"
            android:text="@string/title_timesheet"
            android:textAppearance="?android:attr/textAppearanceMedium"
            android:textColor="@color/colorSection" />

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/timesheet_list_layout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/timesheet_recyclerview"
            android:layout_width="0dp"
            android:layout_height="0dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:listitem="@layout/recyclerview_timesheet_item" />

        <!-- This will be the parent Floating Action Button -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/add_timesheet_entry"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="start|bottom"
            android:layout_margin="16dp"
            android:contentDescription="@string/btn_add"
            android:src="@drawable/ic_check_black_24dp"
            app:backgroundTint="@color/cardview_dark_background"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent" />

        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/view_timesheet_calendar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="end|bottom"
            android:layout_margin="16dp"
            android:layout_marginEnd="108dp"
            android:layout_marginBottom="16dp"
            android:contentDescription="@string/btn_calendar"
            android:src="@drawable/ic_add_white_24dp"
            app:backgroundTint="@color/cardview_dark_background"
            app:layout_constraintBottom_toTopOf="@+id/add_timesheet_entry"
            app:layout_constraintEnd_toEndOf="@+id/add_timesheet_entry"
            app:layout_constraintStart_toStartOf="@+id/add_timesheet_entry" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</LinearLayout>